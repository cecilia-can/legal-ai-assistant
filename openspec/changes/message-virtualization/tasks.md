# 实现任务

## 1. 虚拟化方案与类型边界

- [x] 1.1 评估并锁定支持动态高度的虚拟化方案，验证 React 19、Next.js 16、Tailwind CSS 4 兼容性。
- [x] 1.2 设计 `MessageList` 的虚拟化适配接口，保持现有 `messages`、流式状态和操作回调兼容。
- [x] 1.3 设计内部时间线项扩展类型，为 message、tool-call、tool-result、execution-status 预留 discriminator；本阶段不接入后端 Agent 事件。

## 2. MessageList 虚拟渲染

- [x] 2.1 将 `MessageList` 的消息渲染抽取为稳定的时间线 item renderer。
- [x] 2.2 接入虚拟滚动容器、可见范围计算和 overscan。
- [x] 2.3 支持动态高度测量，并在 Markdown、代码高亮、展开折叠后更新测量结果。
- [x] 2.4 确保消息 key、删除、复制、重新生成和流式消息状态不会导致无关项重建。

## 3. 滚动与流式行为

- [x] 3.1 将 near-bottom 判定、「回到底部」和自动滚动逻辑适配虚拟列表定位。
- [x] 3.2 合并流式 token 带来的测量与滚动更新，避免每个 token 触发完整布局计算。
- [x] 3.3 覆盖切换会话、发送消息、停止生成和流式结束后的滚动状态重置。

## 4. 历史分页与长内容

- [x] 4.1 接入或适配历史消息分页，在顶部插入消息后按锚点恢复视口位置。
- [x] 4.2 为长日志、JSON、代码和未来工具结果增加摘要、折叠和按需展开容器。
- [x] 4.3 让展开/收起只影响当前时间线项的测量和渲染。

## 5. Agent UI 扩展点

- [x] 5.1 实现工具调用、工具结果和执行状态 item renderer 的最小 UI 接口与安全降级。
- [x] 5.2 添加状态、摘要、错误和详情展开的视觉样式，不实现 Agent 执行引擎。
- [x] 5.3 确认左侧 `ConversationList` 保持普通滚动，避免扩大本 Change 范围。

## 6. 验证与文档

- [x] 6.1 添加长列表、动态高度、Markdown/代码、流式更新和历史加载的组件测试。
- [ ] 6.2 手工验收桌面端快速滚动、移动端滚动、向上阅读时流式更新和底部跟随。
- [x] 6.3 运行 `npm run lint`。
- [ ] 6.4 运行 `npm run build`。
- [x] 6.5 更新消息流虚拟滚动与 Agent 时间线扩展的开发/验收说明。
